import { VideoSecurePipe } from './video-secure.pipe';

describe('VideoSecurePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoSecurePipe();
    expect(pipe).toBeTruthy();
  });
});
